// Declare the handlers.rs file as a module within the handlers directory
pub mod handlers;

// This mod.rs file makes the 'handlers' module accessible when the 'handlers' directory is declared as a module.
